package exs;

import javax.swing.*;

/* public class trees <T1, T2>{
        T1 key;
        T2 value;
        trees<T1, T2> left, right;

        trees(T1 key, T2 value) {
            this.key = key;
            this.value = value;
        }
        trees root = null;
    public T2 get(T1 k) {
        trees<T1, T2> x = root;
        while (x != null) {
            int cmp = k.compareTo(x.key);
            if (cmp == 0) {
                return x.value;
            }
            if (cmp < 0) {
                x = x.left;
            } else {
                x = x.right;
            }
        }
        return null;
    }
}*/
